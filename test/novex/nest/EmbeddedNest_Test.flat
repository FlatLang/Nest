package novex/nest

import novex/nest/NestException

import static novex/nest/Nest

testable class {
	test embeddedFunction() {


		// test thisIsTheEmbeddedFunction() {
		// 	expect(5).toBe(4)
		// }
	}

	test lambdaFunction() {
		test("something", {
			expect({ throw NestException("hey there") }).toThrow(NestException.class)
		})
	}
}